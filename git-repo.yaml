apiVersion: environment.cloudbees.com/v1alpha1
kind: GitRepository
metadata:
  name: cloudbees-ci-erenseymen-insurance-frontend                             
  namespace: previews
spec:
  url: https://github.com/cloudbees-ci-erenseymen/insurance-frontend.git
  apiTokenSecretRef:
    name: beedemo-dev-github-token
  autoUpdateContexts:                           
  - default
